# Template — copiez ce fichier en `environments/.env` et remplissez les valeurs.
# Variables communes à tous les modes (souvent vide ; les valeurs spécifiques
# sont à mettre dans .env.development ou .env.production).
