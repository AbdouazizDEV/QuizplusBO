node_modules
dist
.DS_Store
# Ancien emplacement (racine uniquement) — le projet utilise désormais environments/
/.env
/.env.local
/.env.development.local
/.env.test.local
/.env.production.local
# Secrets — les vrais .env du dossier environments/ ne doivent JAMAIS être committés.
# Seuls les fichiers .env.example sont versionnés (templates avec placeholders).
environments/.env
environments/.env.development
environments/.env.production
environments/.env.local
environments/.env.*.local
coverage
*.log
.vite
.cache
*.tsbuildinfo
